name: Unit tests
on: [workflow_call]

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Setup NodeJS
        uses: actions/setup-node@v1

      - name: Install dependencies
        run: yarn

      - name: Test project
        run: yarn test
